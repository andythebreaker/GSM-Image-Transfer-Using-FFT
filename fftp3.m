%clc;clear;close all;
function y=fftp3(x)
%======================================================
%x=[0 1 4 9]';
%======================================================
%y=wary(length(x))*x;
%disp('?');
%disp(size(x));
k=wary(length(x));
%disp('!')
%disp(size(k));
y=k*x;
end